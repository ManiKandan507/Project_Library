import React from "react";
import LocationMap from "./LocationMap";
const ZoomingMaps = (props) => {
    return (
        <div>
            <LocationMap {...props} />
        </div>
    );
}
export default ZoomingMaps;
